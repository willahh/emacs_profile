;; (require 'cl)
;; (require 'php-mode)

;; ;; Add compay-ac-php for company backend
;; (defun wlh/php-mode-hook ()
;;   ;; (company-mode t)
;;   (linum-mode)

;;   ;; flycheck-mode, deja active de maniere global
;;   (flycheck-mode)
;;   (textmate-mode))

;; (add-hook 'php-mode-hook 'wlh/php-mode-hook)
